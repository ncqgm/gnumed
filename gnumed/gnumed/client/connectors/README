# $Source: /home/ncq/Projekte/cvs2git/vcs-mirror/gnumed/gnumed/client/connectors/README,v $
# $Id: README,v 1.7 2007-02-05 16:10:03 ncq Exp $
# $Revision: 1.7 $

This directory stores connectors specific to this client.

Connectors are intermediate programs that 3rd party legacy
applications can use to control a GNUmed instance.

The one provided connector can, for example, be used by any
practice management application in Germany that knows how to
output GDT or BDT files. The connector attaches to GNUmed
via XML-RPC, tells it to load an external patient (which
loads the patient from the BDT/GDT file if properly
configured), locks into that patient and raises a particular
plugin (gmShowMedDocs in this case). If you want your
connector to do something different you can derive a class
from the cBaseConnector class and override the run_script()
method.

The calls you can use on the controlled GNUmed instance are
the methods provided by client/wxpython/gmMacro.py.

Karsten

-----------------------------------------------------------
MDW current patient ID:

 \HKEY_LOCAL_MACHINE\SOFTWARE\Medical Director for Windows\General\CurrentPatient

  - UR_NO field in patient dBase file
  - uppercase 4 letter prefix of lastname
  - uppercase 4 letter prefix of firstname
  - uppercase (?) 1 letter prefix of middlename
  - surrounded by | as delimiters
  - if multiple MDW windows open then it's the patient of the currently *active* one

 \HKEY_LOCAL_MACHINE\SOFTWARE\Medical Director for Windows\General\CurrentDoctor

  - provider number of the currently logged in doctor

-----------------------------------------------------------
TurboMed GDT-Schnittstelle:

From: selbsthilfetm@gmx.net
To: sebastian.hilbert@gmx.net

Sehr geehrter Herr Hilbert,

ups, da hab ich den Mund wohl sehr voll genommen, aber so gut wie die Hotline
sind wir doch alle!

Also ich hab mal für Sie rumgehackt:
1. der Eintrag in der Karteikarte ist ein formulareintrag, der Pfad ist im
Eintrag hinterlegt: "\FORMULARE\KARTEIKARTE\GDT RUECK.TMF" (hoert auf
"tmtest"!). Dieses Formular wird gestartet, wenn sie den blauen Punkt
drücken.

2. gespeicherte datenfelder des formulars sind: datum, zeit, Nummer,
 txtZeile, txtsection, txt8402 - was die im einzelnen machen habe ich nicht
 untersucht, aber...

3. in txtsection ist der Name des Gerätes hinterlegt. Dieser Name wird in der
gdt.ini (\Formulare\karteikarte\) abgefragt und dort das verknüpfte Programm
eingelesen.

4. ebenfalls in der gdt rueck.tmf wird in Zeile 139 (Quellcode) das
 verknüpfte Programm gestartet. Übergabedateien siehe gdt.ini

> Spitze,

So jetzt wissen Sie und ich auch "wat n Dampfmaschin is".

mit freundlichen gruessen
Hugo
-----------------------------------------------------------
MCS-Isynet (Germany):

MCS-ISYNET und Adakta tauschen die Daten über Dateien aus. Die Pfade sind in beiden
Programmen frei konfigurierbar. Bitte verwenden Sie möglichst die Pfade aus dieser
Beschreibung.

Wird in MCS-ISYNET der Patient gewechselt, erzeugt MCS-ISYNET automatisch die Datei
C:\WINACS\TEMP\PATINF01.TMP mit den Patientenstammdaten.

AdAkta importiert bei Aufruf diese Datei. der Import wird durch die Aktivierung des
AdAkta-Fensters ausgelöst, egal wie der Benutzer AdAkta aufruft (ALT+TAB, Mausklick
etc.).

Für den Eintrag in die Karteikarte erzeugt AdAkta nach dem Archivieren eines
Dokumentes die GDT-Datei (ADAKTA.GDT). Der Aufbau der GDT-Datei wird durch die
Formatdatei MCSISY.EXP bestimmt und kann individuell verändert werden.

MCS-KOMAN importiert diese GDT-Datei asynchon (es läuft ein Polling auf die Datei!)
und benachrichtigt dann MCS-ISYNET. Diese Datei wird von MCS-KOMAN gelesen, in die
Karteikarte eingetragen und anschließend gelöscht.

-----------------------------------------------------------
PracSoft/MDW2 (Australia):

Linking with Medical Director

NB: For both versions 1 & 2 .....Make sure you complete the following steps on
every PracSoft workstation that may add or edit data plus on every Medical
Director workstation that you wish to read the link file. At least one Medical
Direcgtor workstation must read the link file. MEDICAL DIRECTOR MUST BE
PHYSICALLY RUNNING ON AT LEAST ONE WORKSTATION THAT IS LINKED, FOR THE
IMPORTING TO OCCUR.

PracSoft will write patient details to the PATIENTS.IN link file every time
you add or edit a patient in PracSoft. It may take up to two minutes for
Medical Director to read and import these details. Generally, the more
workstations reading the link file, the faster the import will occur.

To activate the link from a PracSoft workstation...

  * Open PracSoft (Start - Programs - PracSoft)
  * On the Menu Bar click Setup - Script Export.
  * Click MDW GENERIC ASCII v2. In the "Link File Name" field, type the
    "Patients files director" (taken from step 2), plus the "PATIENTS.IN"
    (e.g. C:\MDW2\PATIENTS.IN)

NB: The November 99 release of Medical Director has a 'Read Link File' program
that can run on your Server, without needing to run an instance of Medical
Director. Contact Medical Director for information on how to implement this.

Linking with Medical Director Version 2

    If you are not using the new facility on the Server, do the following...

      + Open Medical Director.
      + On the Menu Bar click Tools - Options.
      + Click on the Network tab. Write down the "Patients files directory".
      + Click on the Links tab. Click Link to billing package. Set it to
        GENERIC ASCII v2. Set the "Path to transfer file" to to be the same as
        the "Patients files directory". Tick "Read link file from this
        workstation".
      + Click Save.

    TIP: You may need to close Medical Director and open it again for these
    settings to take. You should be prompted if this is required.

Linking with Medical Director Version 1

     1. Open Medical Director.
     2. On the Menu Bar click Options - Network Setup. Write down the Network
        Path (i.e. "the path to the sub-directory where data files can be
        found"). Click OK.
     3. On the Menu Bar click Options - Link Options. Select GENERIC ASCII v2.
        Tick the "Linking to billing package" and "Read link file from this
        workstation". It does not matter whether you tick "Delete link file
        after reading".

        TIP: You may need to close Medical Director and open it again for
        these settings to take. You should be prompted if this is required.

     4. Open PracSoft (Start - Programs - PracSoft).
     5. On the Menu Bar click Setup - Script Export...
     6. Click MDW GENERIC ASCII v2. In the "Link File Name" field, type the
        "Patients files directory" (taken from step 2), plus "PATIENTS.IN"
        (e.g. C:\MDW2\PATIENTS.IN).

-----------------------------------------------------------
